services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: forum
      POSTGRES_USER: forum
      POSTGRES_PASSWORD: forum
    ports:
      - "5445:5432"
    volumes:
      - forum-postgres-data:/var/lib/postgresql/data

volumes:
  forum-postgres-data: